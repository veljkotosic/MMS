<UserControl x:Class="MMS.WpfApp.Controls.FilterStagingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:syncfusion="clr-namespace:Syncfusion.Windows.PropertyGrid;assembly=Syncfusion.PropertyGrid.WPF"
             xmlns:viewModels="clr-namespace:MMS.WpfApp.ViewModels"
             xmlns:local="clr-namespace:MMS.WpfApp.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Filter Staging" FontWeight="Bold" Margin="5"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Filters}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type viewModels:FilterViewModel}">
                        <Border BorderBrush="Gray" BorderThickness="0,0,0,1" Margin="0,0,0,5" Padding="5">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <ComboBox 
                                        Grid.Column="0" 
                                        Grid.ColumnSpan="2"
                                        ItemsSource="{Binding DataContext.AvailableFilterTypes, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        SelectedItem="{Binding SelectedType}"
                                        Margin="0,0,5,5"/>

                                    <Button 
                                        Grid.Column="2" 
                                        Content="↑" 
                                        Command="{Binding DataContext.MoveFilterUpCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Padding="5,0" Margin="0,0,2,5"/>

                                    <Button 
                                        Grid.Column="3" 
                                        Content="↓" 
                                        Command="{Binding DataContext.MoveFilterDownCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Padding="5,0" Margin="0,0,2,5"/>
                                    
                                    <Button 
                                        Grid.Column="4" 
                                        Content="X" 
                                        Command="{Binding DataContext.RemoveFilterCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Padding="5,0" Margin="0,0,0,5"/>
                                </Grid>
                                
                                <Expander Header="Properties" IsExpanded="True">
                                    <Expander.Style>
                                        <Style TargetType="Expander">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasOptions}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Expander.Style>
                                    <syncfusion:PropertyGrid 
                                        SelectedObject="{Binding Options}"
                                         LostKeyboardFocus="PropertyGrid_LostKeyboardFocus"
                                         DescriptionPanelVisibility="Collapsed"
                                         SearchBoxVisibility="Collapsed"
                                         ButtonPanelVisibility="Collapsed"
                                         SortDirection="Ascending"/>
                                </Expander>
                                <TextBlock Text="{Binding ValidationMessage}"
                                           Foreground="Firebrick"
                                           TextWrapping="Wrap"
                                           Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="2" Content="Add Filter" Command="{Binding AddFilterCommand}" Margin="5"/>
        <Button Grid.Row="3" Content="Apply Batch" Command="{Binding ApplyCommand}" Margin="5" FontWeight="Bold"/>
    </Grid>
</UserControl>
